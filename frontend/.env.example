# Azure Static Web Apps → HF ZeroGPU hybrid
# Local dev (make pitch-demo): leave empty → Vite proxy /api → localhost:8000
# Azure prod: set to your HF Space URL (no trailing slash)
VITE_API_BASE=https://imadityasarkar-satquery-ai.hf.space
