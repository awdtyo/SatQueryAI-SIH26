# Training

Fine-tuning notebooks live in `training/notebooks/` (Colab QLoRA) — see AGENTS.md training workflow.
Configs in `training/configs/`, adapter weights in `training/adapters/` (not committed).
